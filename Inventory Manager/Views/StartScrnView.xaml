<UserControl x:Class="Inventory_Manager.Views.StartScrnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory_Manager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="200" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="New_Build" Content="New Build" Style="{StaticResource General_Menu_Style}" Command="{Binding navToNewBuild}"/>
            <Button x:Name="new_component" Content="New Component" Style="{StaticResource General_Menu_Style}" Command="{Binding navToNewComp}"/>
            <Button x:Name="view_inventory" Content="View Components" Style="{StaticResource General_Menu_Style}"/>
            
        </StackPanel>
    </Grid>
</UserControl>
